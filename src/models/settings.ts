export default interface Settings {
  types: {
    guest: boolean
    member: boolean
    moderator: boolean
    owner: boolean
    superChat: boolean
    superSticker: boolean
    membership: boolean
  }
}
